<h1>Results for <%= @searched_item %>: </h1>

  <%# ad_builder(@searched_item.name)%>
  <% @amazon_list[0..9].each do |t| %>
      <div>
    <img src="<%= t['pic']%>" >
    <%= link_to t['title'], t['url'].to_s %>
    <%= t['price']%>
      </div>
  <%end%>
